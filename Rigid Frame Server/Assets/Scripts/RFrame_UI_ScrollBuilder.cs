using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace RigidFrame_Development
{
	
	public class RFrame_UI_ScrollBuilder : MonoBehaviour {

		public RFrame_Bridge bridge;
		public RectTransform scrollContentPanel;

		// Use this for initialization
		void Start () {
			
		}
		
		// Update is called once per frame
		void Update () {
			
		}

		public void updateScrollMenu() {
			// Build up the items in the scroll menu.

			// Check for frame objects


			// Then build up the angle representors for each.


			// Will need to define the vertex types here as well.
		}

	}
}